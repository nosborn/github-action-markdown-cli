---
name: Tag Versioning

on:  # yamllint disable-line rule:truthy
  release:
    types:
      - published
      - edited

jobs:
  actions-tagger:
    runs-on: ubuntu-latest
    steps:
      - uses: Actions-R-Us/actions-tagger@v2.0.2
